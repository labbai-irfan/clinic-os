# ClinicOS — production mode (committed, NON-secret).
# Real production API base is injected by the deploy pipeline; this is a safe default.
VITE_APP_ENV=production
VITE_API_BASE_URL=https://api.clinicos.app/api
VITE_ENABLE_MSW=false
VITE_LOG_LEVEL=warn

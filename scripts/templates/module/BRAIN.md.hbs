#
{{pascalCase name}}
— Module Brain Notes > Module-local decisions, registries, TODOs, and tech debt. Feeds the global >
PROJECT_BRAIN per [BrainRules.md](../../../docs/architecture/BrainRules.md). > Distinct from the
global `Brain.md`. ## Decisions (mini-ADRs) - _YYYY-MM-DD — initial scaffold of `{{kebabCase name}}`
from the canonical module template._ ## Local registries - **Entities defined here:** _none yet_ -
**Services / use-cases:** _none yet_ - **Permissions:** _none yet_ ## TODOs - [ ] Define the Domain
first: `types/` → `schemas/` → `validators/` → `constants/`. - [ ] Build Infrastructure: `api/` →
`mappers/` → `repositories/` → `adapters/`. - [ ] Build Application (`services/`) then Presentation
(`hooks/` → `components/` → `pages/`). - [ ] Curate `index.ts`, wire `routes.tsx` +
`permissions.ts`, add i18n catalogs. ## Tech debt - _none yet_
/**
 * Public API for the ClinicOS Icon Registry.
 *
 * Consume named, categorised icons from here (`import { icons } from
 * '@shared/design-system'`) and render them through the `<Icon>` wrapper.
 * Never import lucide-react directly in feature code.
 */
export * from './registry';

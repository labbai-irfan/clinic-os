/**
 * Public surface for the ErrorState component.
 *
 * Governed by: Phase 6 design-system spec (each component `index.ts` re-exports
 * the component + its props type).
 */
export type { ErrorStateProps } from './ErrorState';
export { ErrorState } from './ErrorState';

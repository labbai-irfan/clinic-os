/**
 * Public surface for the EmptyState component.
 *
 * Governed by: Phase 6 design-system spec (each component `index.ts` re-exports
 * the component + its props type).
 */
export type { EmptyStateAction, EmptyStateProps } from './EmptyState';
export { EmptyState } from './EmptyState';
